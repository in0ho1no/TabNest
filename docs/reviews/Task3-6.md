# Task3-6 クロスモデルレビュー記録

- 実装モデル: Claude Fable 5
- レビューモデル: Claude Opus 4.8
- レビュー対象: `git diff dev-re...Task3-6`
- 実施日: 2026-06-12

## 指摘一覧と対応

| # | 重要度 | 指摘 | 対応 |
|---|--------|------|------|
| 1 | 低 | VM の挿入インデックスがモデル基準で「モデルと VM のコレクションが常にインデックス一致」という暗黙の前提に依存。現状の実装(両者末尾追加・同要素削除)では成立し実害なし。タブ並び替え(v0.2)導入時に再点検 | 対応不要(v0.2 タブ並び替え実装時の申し送り) |
| 2 | 低 | KeyboardAccelerator の設置位置: WinUI 3 の Window 自体は KeyboardAccelerators を持たないため、ルート Grid への設定は SPEC の意図を満たす正しい慣例的実装(確認事項) | 対応不要 |

高・中の指摘なし。

## 確認済み項目(レビュアー所見)

- 復元ルール(元グループ優先 / 消滅・上限時はアクティブグループ / TabIndex 範囲判定)を正確に実装。
  フォールバック時に TabIndex を使わず末尾とする ReferenceEquals 判定も正しい
- 履歴最大20件・古いものから破棄(オフバイワンを最古・最新 Path の具体値で検証)
- 復元成功時のみ履歴を消費(失敗時の非消費をテストで検証)
- 編集中ガードは VM の IsRenameInProgress で no-op(「確定してから実行はしない」の SPEC 意図に合致)
- TabOperationError.NoClosedTab は必要最小限の拡張
- SPEC テスト要件6項目をすべてカバー(Core 10件・VM 4件)

## GUI 評価

- スクリーンショットで確認: 中クリックでタブを閉じ → Ctrl+Shift+T で元の位置(中央)へ復元、
  アクティブ化、フォルダ内容(D:\work)の表示。一時タブはコミット前に削除済み

## 結論

**マージ可(approve)。** 修正を要する指摘なし。
